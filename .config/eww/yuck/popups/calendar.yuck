; calendar
(defwidget cal []
  (eventbox
    :onhoverlost "~/.config/eww/scripts/popup.ts calendar"
    (calendar
      :class "popup"
      ; :day {formattime(EWW_TIME, "%d")}
      ; :year {formattime(EWW_TIME, "%y")}
      :show-details true
    )
  )
)
